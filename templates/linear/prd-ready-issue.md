# <Issue Title>

## Goal

<One paragraph describing the intended outcome. Focus on the user-visible or system-visible change.>

## Context

<Why this matters, what triggered it, and constraints to keep in mind. Optional but recommended for non-obvious issues.>

## Implementation Notes

- Scope: <what is in scope for this issue>
- Files/components expected to change: <list key files>
- Non-goals: <what is explicitly out of scope>
- Edge cases to handle: <known edge cases>

## Dependencies

- blockedBy: <issue identifiers this is blocked by, or "none">
- blocks: <issue identifiers this blocks, or "none">

## Acceptance Criteria

- [ ] Criterion 1
- [ ] Criterion 2
- [ ] Criterion 3

## Validation

- `lint`: required
- `typecheck`: required
- `test`: required
- `build`: required

## Metadata Rationale

- `priority=N (Label)`: <justification — e.g. "2 (High): blocks downstream automation pipeline">
- `estimate=N`: <justification — e.g. "2: single-file contract update with test">
- `estimatedTokens=NNNN`: <projected input+output tokens for implementation — see heuristics in `contracts/ralph/core/issue-creation-defaults.md`>
- `confidence=0.XX`: <0.0–1.0 reflecting scope clarity — higher when scope is narrow and well-defined>
- `lowConfidence=true|false`: <true when scope is ambiguous, cross-cutting, or depends on undocumented behavior>
- `rubricFactors={}`: <JSON object of scorer signal inputs, or `{}` when scored manually>

## Labels

- `Ralph` — domain routing (always required for automation-targeted issues)
- `PRD Ready` — migration compatibility (required during migration period)
- `Agent Generated` — provenance (only when generated by an agent)
